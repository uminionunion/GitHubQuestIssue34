
// This is the configuration file for PostCSS.
// It's a standard JavaScript module export.
export default {
  // The 'plugins' object defines which PostCSS plugins to use.
  plugins: {
    // The 'tailwindcss' plugin processes Tailwind CSS directives and functions.
    tailwindcss: {},
    // The 'autoprefixer' plugin adds vendor prefixes to CSS rules for better browser compatibility.
    autoprefixer: {},
  },
};
